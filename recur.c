#include <stdio.h>
int main()
{


void recursion(int n==5){
    if (n > 0)

        {
        for (int i = 0; i < n; i++) {
            printf("%d", n);
        }
        recursion(n-1);
    }
}
}
